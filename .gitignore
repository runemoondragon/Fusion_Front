# .gitignore

# Dependencies
/node_modules
/.pnp
.pnp.js
# yarn v2+ doesn't typically use .pnp.js in the same way, but keeping for broader compatibility.
# Consider adding /.yarn/ if using Yarn Berry Plug'n'Play, especially the cache and install-state.
# Ignore sensitive and heavy files
/backend/node_modules/
/backend/src/db/
/*.env
# Testing
/coverage

# Next.js specific (Frontend)
/.next/
/out/
next-env.d.ts

# Production / Build output
/build
# Add /dist if your backend build output goes there
# /dist

# TypeScript
*.tsbuildinfo

# Local Environment Variables
# Ignore all .env files by default.
# Specific .env files like .env.example can be force-added if needed: !.env.example
*.env
.env*.local
# !.env.example
# !.env.production
# !.env.development

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# Add any custom log directories/files
# logs/
# *.log

# Operating System specific files
.DS_Store
Thumbs.db

# IDE configuration files
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln

# Miscellaneous
*.pem # Private keys

# Backup pages (as previously defined by user)
app/_backup/

# If you have a public directory for static assets that Next.js doesn't handle in /public,
# and it contains generated files, consider adding it.
# Example: /public/generated/